import React from 'react'

export default function Deepgram() {
  return (
    <div>
        Set Up API Keys: Ensure you have access to your Deepgram API key (and optionally, DeepL API key) and set them as environment variables:
    </div>
  )
}
